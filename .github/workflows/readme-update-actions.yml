name: Update this repo's README

on:
  schedule:
    # Once a day at 8 AM
    - cron: 0 8 * * *

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: IgorWounds/IgorWounds/.github/workflows/readme-update-actions.yaml
        with:
          feed-url: https://algotrading101.com/learn/feed/
          readme-section: feed
